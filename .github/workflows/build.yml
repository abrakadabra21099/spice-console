name: CI

on: [push]

jobs:
  build:

    runs-on: linux

    steps:
    - uses: actions/checkout@v1
    - name: Hello!
      run: echo Hello, world!
    - name: Run a BUILD.
      run: |
        docker-compose build
